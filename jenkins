sudo yum install java-11-openjdk-devel
 java -version
readlink -f $(which java)
 export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-11.0.18.0.10-1.el7_9.x86_64
 export PATH=$JAVA_HOME/bin:$PATH
  echo $PATH
/usr/lib/jvm/java-11-openjdk-11.0.18.0.10-1.el7_9.x86_64/bin:/usr/local/bin:/usr/bin:/usr/local/sbin:/usr/sbin:/home/siri/.local/bin:/home/siri/bin
sudo wget -O /etc/yum.repos.d/jenkins.repo http://pkg.jenkins-ci.org/redhat/jenkins.repo
 sudo rpm --import https://jenkins-ci.org/redhat/jenkins-ci.org.key
 sudo yum install jenkins


by using war 
java -jar jenkins.war
 
 
 cd /var/lib/jenkins ===>default location under that cd workspace/ we can see the jobs
 How to install jenkins by using jenkins.war file
 Download latest jenkins.war file
 run the command java -jar jenkins.war
 https://www.blazemeter.com/blog/jenkins-war-file


master slave 
masternode >>>java install jenkins install


slavenode>>java, jenkins install


Multibranch pipeline

From diffrent branches diiff jenkinfile we can write pipeline at a time

Plugin install  multibranch pipeline>configure jenkinsfile 
 
webhook install plug>configure pipieline>with url in github webhooks paste url enable the webhook>>>Any change happen in git hub it will tigger the pieline 
